const test = async function test() {
	// assert that the hook contains FILENAME
	// fetch FILENAME from github and assert that the file has a doctype
	// and opening and closing html tags.
};

export default test;
